# チーム共通規律(TEAM_STANDARDS)

**全エージェントは作業開始前にこのファイルを読み、これに従う。**
共通ルールの原本はこのファイルだけ。各エージェント定義には役割固有のルールのみを書く(重複させない)。

## 1. データ規律
1. **日付宣言**: 冒頭で「作成日(今日)/ データ基準日(直近営業日など)」を宣言する。相対表現(「本日」「昨日」)をソースから写さず、絶対日付に直す。
2. **時点と出典**: すべての数値に「いつ時点の値か」と「出典」を付ける。
3. **2ソース照合**: 重要数値(指数終値・株価・為替・時価総額など投資判断に効くもの)は独立2ソースで照合する。1ソースしか取れなければ「単一ソース」と注記して先へ進む(検索を繰り返して時間を浪費しない)。
4. **計算式の明示**: 導出値(PER・ウェイト・ランウェイ等)は計算式を明示する(例: PER 33倍 = 株価5,208円 ÷ 予想EPS 158円)。
5. **推測で埋めない**: 取れなかった数値は「**未確認**」と書く。後日フォローが必要な事項は「**要確認**」として宿題化する(未確認=データが取れなかった / 要確認=後日確認が必要な宿題、で使い分ける)。

## 2. 確信度と反証条件
- 確信度は**4段階のみ**: 高(≒80%)/ 中(≒60%)/ 低(≒40%)/ 極低(≒20%)。
  「中〜高」などの中間表記は使わない(キャリブレーション集計を可能にするため)。
- 見立てには必ず**反証条件**(どうなったら間違いと分かるか)を付す。
- 検証結果の判定語は「的中 / 外れ / 判定保留」で統一する。

## 3. 中間成果物と報告
- オーケストレーターに指定された `reports/work/` 配下のファイルに**全文**を保存し、チャットには要旨(5行以内)のみ返す。
- 前回の見立てから変更した場合は「何が変わったから変えたのか」を明示する。

## 4. 免責
- すべての成果物は情報整理・教育目的であり、投資助言ではない。
- 断定的な予言(「上がる/下がる」)をしない。シナリオ・確率・前提条件で語る。
