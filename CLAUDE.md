# 投資リサーチ・分析チーム(日本株・米国株・暗号資産)

毎日の投資情報(日本株・米国株・暗号資産)をまとめ、中期・成長重視(中リスク)の投資家向けに
分析レポートを作成するエージェントチームです。批判的検証と継続的な学習・改善を組み込んでいます。

> ⚠️ **重要**: 本リポジトリのレポート・分析はすべて情報整理および教育目的であり、
> 投資助言(金融商品取引法上の投資助言・代理業)ではありません。将来のリターンを保証するものでも
> ありません。最終的な投資判断はご自身の責任で行ってください。

## チーム構成(`.claude/agents/`)
| エージェント | 役割 |
|---|---|
| `macro-market-analyst` | マーケット概況(指数・為替・金利・海外市場・金融政策・市場内部指標) |
| `news-curator` | 当日の重要ニュースを重要度順に要約(保有銘柄の材料を最優先) |
| `sector-theme-analyst` | セクター・テーマの資金フローと中期の追い風/逆風、テーマ集中リスクの点検 |
| `equity-analyst` | 日本の個別株の財務・成長性・バリュエーション・リスク分析(保有ウェイト順) |
| `us-equity-analyst` | 米国個別株(株式枠・中リスク統合)。ドル建て・米国会計・為替・米国課税を考慮 |
| `crypto-analyst` | 暗号資産(**別枠「高リスク」**)の時価総額・トークノミクス・アンロック・規制・サイクル分析 |
| `red-team-critic` | **批判的検証(レッドチーム)**。データ監査(数値・日付・計算の検証)+論理検証の必須ゲート。精度重視のため `opus` を使用 |
| `retrospective-reviewer` | **学習・改善ループ**。過去の見立てを実績と照合し、キャリブレーション集計と教訓を `LESSONS.md` に蓄積 |
| `report-editor` | 編集長。オーケストレーターが揃えた全出力とレッドチーム検証を統合し、レポートに仕上げる(自分では他エージェントを起動しない) |

## ワークフロー(オーケストレーター=メインエージェントが進行を管理する)
```
TEAM_STANDARDS.md(共通規律)+ プロファイル + LESSONS.md + DECISION_LOG.md の宿題
        │
        ▼
 4アナリスト(マクロ/ニュース/セクター/個別株)が並行リサーチ
   → 中間成果物を reports/work/DATE/ に保存(コミット対象外)
        │
        ▼
 red-team-critic が全出力を検証（必須ゲート）
   → データ監査(日付整合・数値照合・検算) + 反対シナリオ + 宿題チェック + 横断集中点検
   → 重大なデータ誤りは対象アナリストへ差し戻し(最大1回)
        │
        ▼
 report-editor が統合 → reports/YYYY-MM-DD.md
        │              → DECISION_LOG.md に見立てを記録(確信度+反証条件つき)
        ▼
 （週次）retrospective-reviewer が実績と照合
   → キャリブレーション集計 → LESSONS.md 更新 + 手順書の改善提案 + 古いログのアーカイブ
```

## チーム共通規律
**原本は `TEAM_STANDARDS.md`(全エージェントが作業前に読む)。** 要点:
- **データ規律**: 日付宣言 / 全数値に時点・出典 / 重要数値は2ソース照合(取れなければ「単一ソース」と注記) / 導出値は計算式明示 / 未確認は「未確認」、宿題は「要確認」。
- **確信度は4段階のみ**: 高(≒80%) / 中(≒60%) / 低(≒40%) / 極低(≒20%)。中間表記禁止。見立てには必ず**反証条件**を付す。
- **中間成果物**: `reports/work/` 配下に全文保存、チャットには要旨のみ。編集長・レッドチームは原本を読む。
- **宿題の持ち越し管理**: `DECISION_LOG.md` の「検証予定」期日到来分はレポートの「未解決の宿題」で追跡し、放置しない。

## 使い方(スラッシュコマンド)
- `/daily-report` — 日本株の日次レポート。`/daily-report 6758 ソニーG` のように銘柄指定も可。
- `/us-report` — 米国株(株式枠・中リスク統合)。`/us-report NVDA` のように銘柄指定も可。
- `/crypto-report` — 暗号資産(高リスク枠)。`/crypto-report BTC ETH` のように銘柄指定も可。
- `/retrospective` — 過去の見立てを実績と照合し、キャリブレーション集計と教訓蓄積を行う。

## 自動実行(GitHub Actions)
`.github/workflows/auto-research-reports.yml` が cron で自動生成・コミットする。
- 日本株: 平日 16:00 JST(大引け15:30の終値確定後) / 米国株: 米国営業日の翌朝07:00 JST(JSTでは火〜土曜) / 暗号資産: 毎日 08:00 JST / 振り返り: 毎週土曜 09:00 JST
- **有効化の条件**: ①このワークフローを **main にマージ**(cronはデフォルトブランチでのみ発火) ②リポジトリ Secrets に **`ANTHROPIC_API_KEY`** を登録。
- 手動テストは Actions タブの「Run workflow」(対象 jp/us/crypto/retro を選択)。
- ⚠️ サブエージェント多数+Web検索のため実行ごとに API 利用料が発生する。レッドチームは `opus` 使用(コストを抑える場合は `.claude/agents/red-team-critic.md` の `model:` を `sonnet` に変更)。

## 資産クラスとリスク枠
- **株式**: 中期・成長重視・**中リスク**(日本株: `INVESTMENT_PROFILE.md` / 米国株: `US_EQUITY_PROFILE.md`)。
- **暗号資産**: 株式とは**分離した「高リスク」枠**(`CRYPTO_PROFILE.md`)。同じ中リスク基準を流用しない。

## 主要ファイル
- `TEAM_STANDARDS.md` — チーム共通規律の原本(データ規律・確信度・報告方法)。
- `INVESTMENT_PROFILE.md` — 日本株プロファイル(スタイル・保有銘柄・注目テーマ)。**ここを更新すると分析の方向が変わる。**
- `US_EQUITY_PROFILE.md` / `CRYPTO_PROFILE.md` — 米国株・暗号資産のプロファイル。
- `DECISION_LOG.md` — 未検証の見立ての記録(確信度・反証条件・検証予定)。検証済みは `DECISION_LOG_ARCHIVE.md` へ。
- `LESSONS.md` — 蓄積された教訓。全エージェントが作業前に参照する。
- `reports/` — レポート保存先(`reports/work/` は中間成果物・コミット対象外)。

## 設計思想
1. **分業**: マクロ・個別・セクター・ニュースを専門化し、深さと網羅を両立。
2. **批判を制度化**: レッドチームを必須ゲートにし、強気論への流されを防ぐ。データ監査で数値・日付・計算の誤りを毎回検証する。
3. **学習し続ける**: 見立てを記録し実績と照合、確信度段階ごとの的中率(キャリブレーション)を測って是正し、繰り返す失敗は手順書の修正まで踏み込む。
4. **シンプルに保つ**: 共通ルールの原本は `TEAM_STANDARDS.md` の1箇所だけ。エージェント定義には役割固有のことだけを書く。
